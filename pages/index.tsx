import AppHead from "../app/AppHead";
import AppMain from "../app/AppMain";

const App: React.FC = (): React.ReactElement => {
  return (
    <div>
      <AppHead />
      <AppMain />
    </div>
  );
};

export default App;
